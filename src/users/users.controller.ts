import { Controller } from '@nestjs/common';

@Controller('users')
export class UsersController {
  /* 
    GET /users
    Get /users/:id
    POST /users
    PATCH /users/:id
    DELETE /Users/:id
 */
}
